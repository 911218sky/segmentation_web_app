[server]
# 生產環境配置
port = 3012
address = "0.0.0.0"
runOnSave = false
maxUploadSize = 100

[logger]
# 生產環境簡潔日誌
level = "info"
messageFormat = "%(asctime)s [%(levelname)s] %(message)s"

[browser]
# 生產環境瀏覽器配置
gatherUsageStats = false
serverAddress = "0.0.0.0"
serverPort = 3012